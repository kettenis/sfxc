prefix=${prefix}
libdir=${libdir}
includedir=${includedir}

Name: containers
Description: Library with basic container (some are thread safe)
Version: 0.1

Libs: ${libdir}/libcontainers.a
Cflags: -I${includedir}/
